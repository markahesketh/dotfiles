model = "gpt-5.3-codex"
model_reasoning_effort = "high"

[features]
multi_agent = true

[agents.commit]
description = "ALWAYS use this agent for ANY git commit request."
model = "gpt-5.3-codex"
model_reasoning_effort = "low"
developer_instructions = "Use the $create-commits skill and follow its instructions exactly."

[agents.pr]
description = "ALWAYS use this agent when the user wants to create a GitHub Pull Request."
model = "gpt-5.3-codex"
model_reasoning_effort = "low"
developer_instructions = "Use the $create-pr skill and follow its instructions exactly."

[projects."/Users/markhesketh/Code/markahesketh/social"]
trust_level = "trusted"
