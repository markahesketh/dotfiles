name = "commiter"
description = "ALWAYS use this agent for ANY git commit request."
model = "gpt-5.3-codex"
model_reasoning_effort = "low"
developer_instructions = "Use the $create-commits skill and follow its instructions exactly."