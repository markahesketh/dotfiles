name = "commiter"
description = "ALWAYS use this agent for ANY git commit request."
model = "gpt-5.4-mini"
model_reasoning_effort = "low"
developer_instructions = "Use the $commit-creator skill and follow its instructions exactly."